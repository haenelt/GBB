def get_line():

    # get line
    #import numpy as np
    #import nibabel as nb
    #from nibabel.freesurfer.io import read_geometry
    #from lib_gbb.interpolation import linear_interpolation2d
    
    #input_geometry = "/home/raid2/haenelt/projects/GBB/test_data/lh.layer10_def"
    #input_vol = "/home/raid2/haenelt/projects/GBB/test_data/mean_data.nii"
    #n_vertex = 88587
    #line_size = 2 # in mm
    #step_size = 0.1 # in mm
    #direction = 2
    
    #surf = read_geometry(input_geometry)
    #vol = nb.load(input_vol).get_fdata()
    
    
    #coords = surf[0][n_vertex]
    #line = np.arange(coords[direction] - line_size, coords[direction] + line_size, step_size)
    
    #res = []
    #for i in range(len(line)):
    #    linear_interpolation2d(x, y, x_s, y_s, f_s)
    
    
    # input
    # x, y, z
    # volume
    # direction of line 0, 1 or 2
    # step size
    # length
    k = 1